# Verified Runs

Workers write one JSON file per run in this directory.

File names should match the run id:

```text
dashboard/state/runs/run_20260516_umi_001.json
```

Human-readable conclusions should cite `run_id`, not phrases like "latest" or "previous run".
