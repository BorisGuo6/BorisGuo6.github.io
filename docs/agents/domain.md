# Domain Docs

This repo uses a single-context domain layout:

- `CONTEXT.md`: dashboard and control-plane domain language.
- `docs/adr/`: architectural decisions, when a choice should not be re-litigated.

Engineering skills should read `CONTEXT.md` before proposing architecture changes, diagnosing behavior, writing tests, or creating implementation tasks.
