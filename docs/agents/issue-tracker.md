# Issue Tracker

This repo currently tracks work in the dashboard itself rather than a separate issue tracker.

Use `dashboard/state/tasks.json` as the operational TODO source. When creating implementation follow-ups, prefer adding or updating dashboard tasks unless the user explicitly asks for GitHub Issues.

GitHub remotes exist, so this can be switched to GitHub Issues later without changing the dashboard domain model.
